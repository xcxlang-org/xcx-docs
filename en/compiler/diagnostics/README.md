# Error Engine Diagnostics (`src/error/`, `src/sema/error/`)

The reference document containing structured output definition methods mapping stream-based error reporting within compiler invocations vs dynamic runtime execution error reporting during command interpretations.

## Directory Index

- **[compiler_errors.md](compiler_errors.md):** Reference information for the CLI assistant named `Reporter`, dealing in structural annotations mapped via `Span`, failure verification levels assigned via `Severity`, handling classification color windows mapping visual code blocks (colored squiggles around loops/instances). Also documents exceptions like C-style comments mapped for the lexer, identifying block matches between S101-S302 semantic input mismatch routines, or machine-critical E300-E399 halt stop definitions blocking machine pipeline loops.
